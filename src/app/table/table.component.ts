import { Component } from '@angular/core';

//  Display at least the following fields: ID, Name, Type(s), and Sprite (image).

@Component({
  selector: 'app-table',
  standalone: true,
  imports: [],
  template: ` <p>table works!</p> `,
  styleUrl: './table.component.css',
})
export class TableComponent {}
